import React from 'react';
//import './styles/ProjContent.css';

const ProjContent = ({children}) => {
    return(
        <div className="latios">
            {children}
        </div>
    );
}

export default ProjContent;
